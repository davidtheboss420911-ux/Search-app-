version: 2.1

jobs:
  build:
    docker:
      - image: cimg/android:2026.08

    steps:
      - checkout

      - run:
          name: Build Android App
          command: |
            chmod +x gradlew
            ./gradlew assembleDebug